# Database connection (shared with zero-hono-react-counter-uplot)
# Must point to a postgres instance with wal_level=logical
DATABASE_URL="postgresql://user:password@127.0.0.1:5432/postgres"

# Electric service URL (docker-compose exposes on port 30000)
ELECTRIC_URL="http://localhost:30000"

# Development server
VITE_DEV_PORT=3100
